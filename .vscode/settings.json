{
    "emeraldwalk.runonsave": {
        "autoClearConsole": true,
        "commands": [
            {
                "cmd": "${workspaceFolder}/.copy-on-save.sh ${file} ${relativeFile}"
            },
        ]
    }
}